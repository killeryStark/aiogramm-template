**TELEGRAN BOT TEMPLATE ON AIOGRAM**

Установка зависимостей:
```
$ pip install requarements.txt
```

Создание переменных окружения через файл .env
```
$ cp .env.dist .env
```

Добавить переменные окружения в .env или в ручную

Запуск через файл `app.py`